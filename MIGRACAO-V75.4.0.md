# Migração V75.3.0 → V75.4.0

Substitua o `index.html` pela versão V75.4.0. Os dados existentes do Recruitment Hub permanecem na mesma chave local `scout_v750_recruitment_<workspace>`. A projeção continua em `clubProjections`; ao aplicar uma sugestão, apenas `formation` e `slots` da projeção do clube são atualizados.

Não há migração destrutiva de jogadores, avaliações, equipes, temporadas ou reuniões.
