# Migração V72.9.2

1. Faça backup pela Central 1.0.
2. Substitua `index.html`, `sw.js` e `manifest.json` no mesmo commit.
3. Aguarde o GitHub Pages concluir o deployment.
4. Atualize com `Ctrl + Shift + R`.
5. Confirme **V72.9.2 / 1.2.9.2** no sistema.
6. Teste o filtro de nível no Funil e o botão **Novo atleta** em zoom 100%.

Não há migração obrigatória de dados nem alteração de regras do Firestore. O campo `level` já existente é reutilizado.
