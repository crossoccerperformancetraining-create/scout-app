# Migração V73.1.34

Publique no mesmo commit: `index.html`, `sw.js` e `manifest.json`.

Depois do deploy, use Ctrl+Shift+R. PDFs antigos não mudam retroativamente: gere novamente o PDF/card para receber os novos links e QR.
